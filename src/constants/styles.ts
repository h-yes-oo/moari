import styled from 'styled-components';

import palette from 'constants/palette';

export const BoldLargeText = styled.div`
  font-size: 28px;
  font-weight: bold;
`;

export const BoldMediumText = styled.div`
  font-size: 24px;
  font-weight: bold;
`;

export const TagText = styled.div`
  color: ${palette.primaryViolet.toString()};
  margin: 0px 4px;
`;
